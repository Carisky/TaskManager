/**
 * Объект конфигурации для клиентской части приложения.
 * @typedef {Object} ClientConfig
 * @property {string} host - Адрес сервера, к которому осуществляется обращение.
 */

/**
 * Конфигурация клиентской части приложения.
 * @type {ClientConfig}
 */
const config = {
  host: "http://localhost:3001", // Адрес сервера
};
export default config